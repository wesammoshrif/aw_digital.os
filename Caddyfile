# Live-Domain: os.awcode.de → A-Record zeigt auf den Hostinger-VPS
# (187.124.190.135). Caddy holt sich dafür automatisch ein
# Let's-Encrypt-Zertifikat (Port 80/443 müssen offen sein).
os.awcode.de {
	encode zstd gzip
	reverse_proxy app:3000
}
